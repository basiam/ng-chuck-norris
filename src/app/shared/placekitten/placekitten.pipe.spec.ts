import { PlacekittenPipe } from './placekitten.pipe';

describe('PlacekittenPipe', () => {
  it('create an instance', () => {
    const pipe = new PlacekittenPipe();
    expect(pipe).toBeTruthy();
  });
});
